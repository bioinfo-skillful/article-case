interface:
  display_name: "OpenScience Case Operator"
  short_description: "Operate research conversations and assess evidence"
  default_prompt: "Use $openscience-human-review to allocate decisions before launch, operate the authorized case, and retain evidence for independent assessment."
policy:
  allow_implicit_invocation: true
