# NHANES_2015_2016_SMOKING_PHQ9: Human Review Operating Prompt

Run only after the user authorizes execution. Label all external operations and assessments **Human Review**, without external model or agent identity labels. Observe dynamic autoReview during scientific execution and quantify its effects afterward. Do not launch the separate internal Reviewer for frozen artifacts. Keep external evaluation rules outside OpenScience.

## Case conditions

- Root: `E:/codex/open-science-server/cases/nhanes-2015-2016-smoking-phq9`; reserved attempt: `run_001`.
- Input: English file 01 only. No user data attachments exist. OpenScience retrieves data after Plan approval.
- Enable Plan-first and dynamic autoReview from the first submission through execution. Read back effective settings. Record the actual execution model, reasoning effort, product commit and internal autoReview routing at CP0; do not inherit historical case settings or claim configuration before verification.
- Proposed budget: 15 minutes for minimal readiness checks; a 30-minute target for Plan generation; 120 minutes after approval for scientific execution and natural checks/corrections. Allow at most two scientific Plan-feedback rounds, then pause for a user decision. Download each file at most three times, with a 60-second timeout per request. Confirm these proposed limits when execution is authorized. No timer has started. Include human waiting in total wall time and record it separately.
- At the execution limit, preserve existing outputs and stop only this case through a supported safe interface. If safe stopping is unavailable, record the state and request handling.
- Human Review performs the initial assessment; the user reviews it and approves an exact Plan version. Preparing these files does not approve a scientific Plan.
- Reproduction is designed but not authorized to run. Bare-run comparison and probes are disabled. No standalone internal Reviewer is scheduled.

## 1. Prepare and submit: CP0

Read 01–04 and the preserved master checklist in the design-basis directory. Freeze external methods, scope and denominators. Submit only 01, genuine attachments if any, and necessary verified scientific environment facts. Check existing registration before submitting; never duplicate an uncertain submission.

Verify necessary software, source connectivity and file paths. Record effective versions, configuration and working directory. Use the current CLI help; do not invent commands or UI labels. Prepare real screenshot capture and native event/version retention before starting. If essential capture fails, attempt permitted recovery; if still blocked, explain the specific evidence gap and obtain a decision before continuing.

Keep autoReview enabled. A setting alone does not prove a check occurred. If Plan-stage checks occur, record them separately from scientific-output checks. Record actual observation limitations; never disable autoReview merely to create a pre-review snapshot.

## 2. Plan and scientific execution: CP1–CP3

Save complete native `plan_v1`, associated rules, version and timestamp before feedback. Perform and freeze its initial assessment. Request changes only for scientific errors, omissions of public requirements or execution blockers. Retain reasonable alternatives and merely optional improvements.

Before approval, only official-documentation reading and package/network/field checks are allowed. Do not fit models, calculate associations or execute analysis and retrospectively call its description a Plan. After the user approves an exact version, perform the product approval action and verify execution actually begins.

Monitor real status, screenshots and native history during scientific execution and dynamic autoReview. Do not insert a human freezing/registration round or supply independent reference answers to this measured run. Material scientific-scope changes require the user's decision; record any human scientific assistance separately.

Associate each observed check with its actual context, file/code versions, tool feedback, reads and scope. Relevant objects include executed code, DEMO/SMQ/DPQ inputs and documentation, derived variables, survey design and populations, prevalence/models tables, figures and report. A manifest-only check is not scientific review. Preserve natural access failures as baseline outcomes; any authorized assisted rescue is a separate attempt and cannot overwrite them.

## 3. Freeze after completion: CP4

Confirm execution and natural checks/corrections have ended or explicitly terminated and files are stable. Copy scientific originals into `delivery_final` and verify hashes. Preserve the original product verdict, termination reason, configuration, autoReview events and available version history. Blocked or unreviewed outputs may be frozen without being called a pass.

Retrieve genuine pre-change history when available, retaining original generation time and actual collection time. Missing history stays `missing`; do not reconstruct `delivery_initial` from final files. Local diffs support only their actual scope. Do not repair files under evaluation.

## 4. External quantification

Execute 02 under 04. Independently assess final outputs and freeze the issue list before detailed association with autoReview history. Report residual issues and confirmable misses; evidenced detection/change chains; true findings, false positives, suggestions and unresolved findings; and correct repairs, unchanged issues, unsuccessful changes, harmful changes and indeterminate outcomes. The user reviews Human Review's assessment. Later assisted repairs must use separate versions and must not change the frozen baseline evaluation.

## 5. Optional reproduction and archive: CP5–CP7

Only execute reproduction when authorized, within 04's boundary. Freeze new outputs before comparison. Record execution path, environment and assistance separately. File 02 collects existing reproduction evidence and does not authorize a new run. Preserve screenshots under 03 and all attempts/gaps under 02. Token collection is optional and must not delay the case.

## Paths and separation of external materials

The case root stores preparation/evaluation materials; do not expose the entire root as the scientific project. Prefer `run_001/science_workspace` as the scientific directory. At CP0 verify the actual Notebook root and host export mapping; submit only 01 and the verified scientific path. Keep START, 02–04, design-basis files and `human_records` out of attachments and project indexes. If access is not technically isolated, disclose the limitation; reading reference answers contaminates the baseline and must be recorded.

`run_001` is reserved, not started. Check registration before creating a session. Record project/session IDs when available and actual run IDs on submission. This file is an operating prompt, not an executable CLI script.

Human Review handles CLI operations, monitoring, screenshots, retention, initial assessment and independent verification. Do not restart shared services or automatically alter global proxy settings. Record a durable `human_records/run_001/handoff.md` after significant transitions: actual IDs, state, observation time, versions, last/next screenshot, pending decision, budget and next action. Before execution, record IDs as unregistered rather than inventing them.


Current execution adaptation: [run_001/OPERATING_ADDENDUM.md](run_001/OPERATING_ADDENDUM.md). Original supplied edition retained under original-package.
